streamlit==1.25.0  # Replace with the latest compatible version of Streamlit
requests==2.31.0   # Replace with the latest compatible version of Requests
beautifulsoup4==4.12.2  # Replace with the latest compatible version of BeautifulSoup
